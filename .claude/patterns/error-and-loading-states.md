# Error and loading states in the UI

_Group: error-handling_

Every data-fetching view has explicit states: loading, error, empty. Log errors and show the user a friendly message, never a raw stack trace.
