### About this folder

- Purpose: Shared custom React hooks and data fetching logic.
- Note: Data fetching hooks are organized by feature (e.g., `posts/`, `users/`) and integrate with `src/schemas`.
- Conventions: Follow the pattern in `.cursor/rules/template/data-fetching.mdc` (separate `keys`, `client`, and `server` files).
